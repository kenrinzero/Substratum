# NORMALIZERS.md — registry + unit manifest

One row per normalizer. A unit is dispatchable only when its `deps` are
already vendored/pinned and its fixture plan is satisfiable without
`F:\game` access (DESIGN.md § 5). One unit = one format:
`substratum/formats/<format>.py` + fixtures + expected manifests +
`tests/test_<format>.py` calling `substratum.verify.run_checks`. Every
routine unit carries a differential tool OR the self-consistency proof —
structural-only green does not exist here.

**Fixture tiers (DESIGN.md § 5):** `synthetic` (committed) ·
`homebrew` (web-fetched, committed if license-clean, else `fixtures/_local/`) ·
`FIXTURE REQUEST` (Kenrin drops one retail file into `fixtures/_local/`
at his convenience; never committed, metadata only in outputs).

| format | kind | tier | status | fixture plan | differential (pinned at unit) | deps | notes |
|---|---|---|---|---|---|---|---|
| toyfs | synthetic container | — | HARNESS-ONLY (committed at seed) | `fixtures/toy/` | generator-authored reference bytes | none | exists to prove the gate bites; never extend it |
| iso9660 | disc filesystem | T2 | **GREEN** (2026-07-17, `substratum/formats/iso9660.py`; record/path hardening + retail anchor 2026-07-26; GD `lba_base` + single-sided both-endian tolerance 2026-08-14) | synthetic via pycdlib (`fixtures/iso9660/synthetic/`, seedtool-authored) + homebrew SuperTux PS2 CD (`fixtures/iso9660/supertux/`, 779 files, GPL-2.0, provenance in dir) + retail metadata anchor **Gallop Racer 2001 (USA)** (`SLUS-20255`, 790,986,752 bytes, 10 files, public CRC32/MD5/SHA-1 exact; `seedtools/stage_iso9660_retail_anchor.py`); retail ISO and extracted references stay gitignored, only provenance + manifest commit | 7-Zip **26.02 (x64) 2026-06-25** (`7z l -slt -tiso` / `7z x -tiso`) + pycdlib **1.16.0** as second reader; fidelity sample seed **1** | none new | PVD-only by scope (no Joliet/RR; mode2/2352 is ps1-bincue's substance); refuses multi-extent/interleave/XAR/BE-mismatch, directory records that exceed their extent or cross a logical block, and empty/dot/traversal path components structurally; `stage_homebrew_iso.py` and `stage_iso9660_retail_anchor.py` re-run the two-reader acceptance check. **`lba_base` (2026-08-14, GD-ROM composition):** `normalize_iso9660(src, *, lba_base=K)` translates disc-absolute extent locations into track-relative space — Dreamcast GD data tracks master every extent +45,000 (verified byte-exactly on staged SA2: PVD root record, both path tables, all directory-record references; descriptors stay track-relative; the volume space stays the track size; the base is self-described by the track's sector-0 MSF, exposed as `saturn_dc_raw.lba_base()`). Default 0 is a byte-for-byte no-op; an extent below the declared base is a structural red, never a silent misread. Proven by mutation (GD-style re-master of the synthetic fixture walks to the identical FileTree) and on retail SA2 (2,572 files, 137 loose ADX, Stratum sweep `cri.adx`=137 + `cri.afs`=3). **Single-sided both-endian tolerance (2026-08-14, RE4 PAL):** when a record's LE and BE extent/size disagree, the side whose pair is structurally possible (in track-relative space) wins — and supplies the size too; both-plausible or neither-possible disagreements still refuse. Characterized on the staged 2021 re-master (one corrupt record, `IOPRP.IMG;1`, LE extent = the volume space; 7-Zip itself errors on the disc); mutation-proven (identical FileTree) and retail-proven (9-file tree, BE values) |
| gc-fst | disc filesystem | T2 | **GREEN** (2026-07-22, `substratum/formats/gc_fst.py`; path hardening 2026-07-26) | `FIXTURE REQUEST` retail drop "The Hulk" (`GHKE7D`, NTSC/USA, 1,459,978,240 bytes) in gitignored `fixtures/_local/The Hulk (USA).iso` (**renamed 2026-08-14** from the uninformative `game.iso`; content and sha256 `dd1aeb23…` unchanged, so the committed manifest still verifies. Note the *nested* synthetic fixture at `fixtures/gc_fst/nested/game.iso` keeps that name and is a different file); flat FST (11 files, no nested dirs); test skips cleanly when ISO absent — only the metadata manifest commits. **+ nested-fixture proof-strengthening** (2026-07-22): hand-authored 4-file/2-dir nested FST exercising the recursive traversal (`fixtures/gc_fst/nested/`, built on demand by `seedtools/make_gc_fst_nested_fixture.py` from the Hulk's sys region; genuine wit two-party differential on nested bytes — upgrades the flat fixture's self-consistency proof; only the manifest commits) | wit **v3.05a r8638 cygwin64** (vendored 2026-07-17 → `tools/wit/`; `wit files-ll` for listing, `wit extract` for reference bytes); sole differential tool + DESIGN §3 structural self-consistency proof (no second GC-FST reader exists); fidelity sample seed **1** (all files, ≤16) | none left (wit vendored) | FST-only scope (user-data files; `sys/` virtual header regions not synthesized); refuses Wii/TGC/malformed and empty/dot/traversal path components structurally; FST nodes are **0x0C not 0x20** (load-bearing yagcd §13.4 finding); directory nesting supported by parser AND exercised by the nested fixture (recursive close/resume + sibling-after-subtree proven on real bytes); **test-layer memory discipline fixed 2026-07-25** — structural-red tests now stream-copy + patch-in-place instead of `bytearray(iso.read_bytes())`; with the matching verify.py check-4 streamed compare, the gc-fst module's peak RSS dropped from **~2.8 GB to ~45 MB** (the normalizer was always lazy; the bugs were in the test harness's mutant construction and the gate's own eager fidelity compare). **+ memory-discipline gate** (2026-07-25, `tests/conftest.py`): autouse per-test peak-RSS budget (1 GB, sampled via psutil daemon thread) that makes any future whole-fixture read die loudly instead of silently peaking RAM — the four-check gate cannot see memory; this one can. Red-team proven (`tests/test_memory_gate.py`); gate's bite verified by reverting `verify._first_diff` to eager and watching the gate catch the 1.09 GB peak. **+ authoring memory fix** (2026-07-26): the nested seedtool now truncates a sparse output and seek-writes real regions instead of allocating a 1.46 GB bytearray; regression-tested and byte-identical to the prior image. |
| chd | container (decode) | T2 | **GREEN** (2026-07-18, `substratum/formats/chd.py`; retail closure 2026-08-21: DVD extractdvd + temp-source.path exposure for ps1-bincue composition) | self-staged from the iso9660 unit's SuperTux PS2 ISO via `chdman createcd` → `fixtures/chd/supertux/supertux.chd` (8,492,240 bytes, sha256 8aff4f64); expected manifest derived from pre-compression iso manifest (independently authored at S1); reference bytes = iso reference (decompressed disc byte-identical); **retail DVD-type proof**: staged gitignored `fixtures/_local/7 Wonders of the Ancient World (USA).chd` (PSP UMD DVD-type, `Tag='DVD '`, 2048-byte units, zstd, sha256 `3f91c7ab…`) walks to 22 files via direct `chd → iso9660`; **retail CD-type composition proof**: `fixtures/_local/bin-chd-playstation/BursTrick - Wake Boarding!! (USA).bin` round-tripped through `chdman createcd → normalize_chd → normalize_ps1_bincue → normalize_iso9660` yields identical FileTree to direct ps1-bincue walk | chdman **0.288 (mame0288)** (vendored 2026-07-17 → `tools/chdman/`) — tag dispatch: `extractdvd` for `Tag='DVD '`, `extractcd` otherwise — + inner walk vs the pre-compression iso manifest + BursTrick round-trip byte spot checks; fidelity sample seed **1** | none left (chdman vendored) | returns ByteView; dispatches chdman by metadata tag (`Tag='DVD '` (whitespace-padded) → `extractdvd` (2048-byte plain image) → direct iso9660 composition; else `extractcd` (`.cue` + `.bin` siblings) → ps1-bincue composition. Installed-wheel discovery: authoritative `SUBSTRATUM_CHDMAN` executable → `PATH` → repo-local fallback. Extracted temp source `_TempFileSource` supports explicit idempotent `close()` / context management with `weakref.finalize` fallback, AND **exposes a `path` attribute** naming the extracted `.bin` so sibling files (the CD-type `.cue`) are resolvable by composed normalizers (ps1-bincue). The CD-type composition chain is `normalize_chd → normalize_ps1_bincue → normalize_iso9660`; the ps1-bincue resolver accepts any `.path`-bearing ByteSource via duck typing, not strictly `FileSource`. **PS1 scope note**: ps1-bincue refuses multi-track (~32% of PS1 titles), so CD-type CHD reach caps near 4,100/6,089 — an intentional limit, not a bug in chd. `chdman info` metadata probe wraps subprocess errors including `TimeoutExpired` so transient failures fall back to the safe CD path. |
| ps1-bincue | disc image (mode2) | T2 | **GREEN** (2026-07-23, `substratum/formats/ps1_bincue.py`; retail anchors + general Form-2 support + MSF/CUE hardening strengthened 2026-07-26) | synthetic via pycdlib XA ISO (`fixtures/ps1_bincue/synthetic/`, seedtool-authored) — pycdlib authors a PS1-shaped XA ISO (interchange_level=1, xa=True, SYSTEM.CNF-style boot stub + DATA + DATA/SUB + APP.ELF + 0-byte file + sha256-chain blobs); seedtool wraps each 2048-byte block into a 2352-byte Mode 2 Form 1 raw sector (sync + BCD address + XA subheader + user data + CRC-32 EDC + zero-fill ECC) and writes `game.bin` + `game.cue` (single data track MODE2/2352, INDEX 01 00:00:00); tests derive deterministic mixed Form-2 sectors from that committed carrier. Retail metadata anchors under gitignored `fixtures/_local/`: Redump-matching **King's Field (Japan)**, 12,916 sectors with zero-payload Form 2 only at sectors 12-15 and 470 ISO9660 entries (`seedtools/stage_ps1_retail_anchor.py`); Internet Archive-matched **BursTrick - Wake Boarding!! (USA)**, 157,246 sectors with 60,666 genuine Form-2 sectors across STR/XA extents and 9 ISO9660 entries (`seedtools/stage_ps1_form2_retail_anchor.py`). Both publish only provenance + manifest and locally extract gitignored references. | chdman **0.288 (mame0288)** (vendored 2026-07-17 → `tools/chdman/`; structural anchor via `createcd` + `verify`) + pycdlib **1.16.0** byte-differential + 7-Zip **26.02 (x64) 2026-06-25** for post-remap ISO listing/reference extraction; direct raw-offset comparison independently checks complete 2048/2324-byte sector payloads; fidelity sample seed **1** | none left (chdman vendored, pycdlib dev-only) | ECMA-130 / Yellow Book Mode 2: Form 1 is 12 sync + 4 header + 8 XA subheader + 2048 user + 4 EDC + 276 ECC; Form 2 is 12 + 4 + 8 + 2324 user + 4 EDC = 2352. The stdlib `Mode2XASource` returns one `ByteView`: ordinary `read_at()` remains the fixed 2048-byte cooked stream required for ISO9660 LBA composition, while public `read_sector()` preserves the complete 2048/2324-byte payload plus XA file, channel, submode, and coding metadata. Encoded XA audio/video payloads are exposed but codec decoding remains downstream. The eager structural pass validates sync, mode, byte-identical repeated XA subheaders, BCD field/range correctness, and monotonically advancing MSF from an arbitrary valid starting address on every sector; a one-byte-per-sector form map and one-sector payload cache avoid materialization. The CUE parser fails closed on unmatched/malformed lines and duplicate INDEX declarations. Refuses multi-track, audio-track, non-MODE2/2352, bad INDEX 01, non-sector-aligned BIN, missing CUE sibling, sync mismatch, mode != 2, invalid/non-contiguous BCD/MSF, and subheader disagreement; Mode 1, CD-DA, CD-TEXT, and subchannels remain out of scope. |
| saturn/dc-raw | disc image (raw 2352) | T2 | **GREEN** (2026-07-23, `substratum/formats/saturn_dc_raw.py`; homebrew proof + MSF hardening 2026-07-26) | synthetic via pycdlib (`fixtures/saturn_dc_raw/synthetic/`) plus GPL-3.0 Slinga Save Game Copier **3.7.1** (`fixtures/saturn_dc_raw/save-game-copier-3.7.1/`, 412 sectors, 49 files, provenance and license committed). Pinned UNECM reconstructs complete raw sectors from the upstream MODE1/2048 release ISO; pinned ECM requires 412 Mode-1 sectors and zero literal/Mode-2 records, then round-trips the raw image byte-exactly. | ECM/UNECM **v1.3.0 binaries from the pinned v1.3.1 release asset** (raw EDC/P-Q ECC reconstruction, classification, and round-trip) + 7-Zip **26.02 (x64) 2026-06-25** (`7z l -slt -tiso` / `7z x -tiso` on the remapped inner ISO) + pycdlib **1.16.0** as second reader; fidelity sample seed **1** | none left (ECM provisioner committed; binaries gitignored) | 2352 CD-ROM Mode-1 raw, single contiguous data track, no cue; refuses multi-track/audio/2048-raw structurally; returns ByteView, caller composes with iso9660 (DESIGN §1). 2048-raw is iso9660's domain (no separate unit). Runtime validates sector length, sync, mode, BCD field/range correctness, and monotonically advancing MSF from any valid starting address. Eager stdlib EDC/P-Q ECC enforcement was benchmark-rejected; pinned ECM owns complete error-code validation at staging. **GD-ROM high-density minutes (2026-08-14, Stratum consumer ask):** minutes ≥ 100 (real DC data tracks reach 0xC2 = 122) continue the BCD pattern with hexadecimal tens digits (0xA0 = 100 …), characterized on a staged SA2 dump; the minute's high nibble 0xA–0xF is accepted while second/frame stay strict BCD — genuine Saturn media caps at 99:59:74, and per-sector MSF contiguity remains the structural gate (verified: the eager pass validates all 504,150 SA2 sectors). **`lba_base(track)` (2026-08-14):** derives the track's disc-absolute starting LBA from sector 0's address (0 for Saturn CDs at 00:02:00; 45,000 for GD high-density at 10:02:00) — the GD composition is `normalize_iso9660(view.source, lba_base=lba_base(track))`; retail-proven on staged SA2 (2,572-file tree, Stratum sweep `cri.adx`=137 + `cri.afs`=3). |
| cso | container (decode) | T2 | **GREEN** (2026-07-23, `substratum/formats/cso.py`; retail anchor strengthened 2026-07-26; sniff tightened to the full v1 header shape 2026-08-21 for the `ciso` disambiguation) | synthetic: maxcso compresses the iso9660 synthetic disc to `game.cso` (its incompressible sha256-chain blobs force stored-raw blocks alongside compressed ones); reference = the iso9660 synthetic fixture's `reference/` (inner disc byte-identical); only `game.cso` + manifest commit. Retail metadata anchor: Internet Archive item [`psp-cso-eu`](https://archive.org/details/psp-cso-eu), original member `Ape Escape (EU).cso` (396,382,256 bytes; published CRC32/MD5/SHA-1 exact), internal `UCES00045`; `seedtools/stage_cso_retail_anchor.py` publishes only provenance + manifest and locally extracts gitignored references. | maxcso **v1.13.0** (vendored 2026-07-23 → `tools/maxcso/`; `--decompress` round-trip = structural anchor) + pycdlib **1.16.0** inner-ISO byte-differential + 7-Zip **26.02 (x64) 2026-06-25** retail listing/reference extraction; **stdlib zlib decode**; fidelity sample seed **1** | maxcso (vendored) | CISO **v1 only** (magic `CISO`, u32 index, 2048B blocks, top-bit stored-raw, `align`/index_shift honored, ABSOLUTE file offsets); ZSO/CSO-v2/DAX refused by magic/version; returns ByteView, caller re-normalizes with iso9660 (DESIGN §1). Resolves the old `umd/psp-iso` row: a plain UMD/PSP `.iso` is 2048B ISO9660 already walked by `iso9660` — no separate unit; the compressed CISO container is the substance. The local retail gate additionally hashes the complete 801 MB decoded stream against maxcso truth before checking `PARAM.SFO` and `UMD_DATA.BIN` identity. **Sniff disambiguation (2026-08-21):** the GC/Wii compact ISO (the `ciso` row) shares the `CISO` magic — the sniffer now requires the full PSP v1 header shape (LE header_size 0/24 at 0x04, LE block_size 2048 at 0x10, version 1 at 0x14) instead of magic-only, and `ciso` is registered before this row. |
| ciso | container (decode; GC/Wii compact ISO) | T2 | **GREEN** (2026-08-21, `substratum/formats/ciso.py`) | synthetic via `seedtools/make_ciso_fixture.py` (`fixtures/ciso/synthetic/` — hand-packed: the committed iso9660 synthetic disc padded to one 2 MiB slot + a sha256-chain slot at block 2 = slot 1, block 1 deliberately absent so slot j ≠ block index j; expected manifest entries from pycdlib's own records, source identity = the decoded view per the spooled-container convention) + retail anchor **Luigi's Mansion (USA, Canada)** `.ciso` (operator-staged into gitignored `fixtures/_local/`, 199,262,784 bytes, sha256 `a868fd4b…`, disc `GLME01` — NKit 2's wit-compatible container with an appended 0x240-byte `NKIT  v2` recovery trailer); `seedtools/stage_ciso_retail_anchor.py` authors the manifest from wit's independent `files-ll` listing + `extract` (847 files / 64 dirs) and pins the view sha256 computed by its own minimal slot placement — never the normalizer | **wit v3.05a r8638 cygwin64** (reads AND writes the container; the wit-authored round-trip `Hulk.iso → wit CISO → normalize_ciso` equals wit's own decode **byte-exact over the full 4,699,979,776-byte image**) + retail gate through the `ciso → gc-fst` composition (wit-authored manifest + extraction) + **characterized wit scrub**: `wit copy` zeroes GC junk (drops all-junk blocks, zeroes junk spans in stored blocks) — on the NKit-authored Luigi anchor the wit decode differs from the view on exactly blocks 1 and 604, every difference one-directional nonzero→zero and never inside a game file; fidelity sample seed **1** | none new (the format is a pure block remap — stdlib `struct`, nothing to decompress) | Layout characterized empirically against wit's own reader AND writer 2026-08-21: magic `CISO`, LE u32 block size `0x200000` at 0x04 (wit/NKit both; anything else refused), byte-per-block presence map at 0x08 (0/1 only; unknown values refused), fixed 0x8000 header area (nonzero bytes past the 2242-entry map refused), present blocks' payloads as RAW 2 MiB slots packed in ascending block order from 0x8000, then EOF or an `NKIT`-magic recovery trailer (≤ 1 block; wit ignores it). **The total is declared nowhere** — wit's reader always reconstructs the fixed single-layer Wii size (4,699,979,776; verified: `wit COPY` of a GC-content CISO emits a 4.7 GB ISO), so the ByteView reports that size and absent blocks read as zeros; dual-layer Wii CISOs are untested and out of scope (a file with slots beyond the map fails the trailing-data check). **The disambiguation this unit existed for:** PSP CISO v1 (`cso`) shares the magic — both sniffers key on the LE u32 at 0x04 and `ciso` is registered before `cso`, so the families can never cross-dispatch (both directions test-pinned). Returns ByteView; caller composes `ciso → gc-fst` (GC content occupies a block prefix). |
| wii-u8-arc | archive-as-FS | T2 | **GREEN** (2026-07-24, `substratum/formats/wii_u8_arc.py`; path hardening 2026-07-26) | synthetic (tier-1, seedtool-authored) `fixtures/wii_u8_arc/synthetic/` | structural self-consistency (DESIGN §3); fidelity sample seed **1** | none new (stdlib `struct`) | U8 node archive -> FileTree; 12-byte nodes, 1-based child scopes, absolute file offsets; refuses YAZ0/SZS/other magics, bad tag, out-of-bounds files, cycle/self-parent dirs, non-ASCII names, and empty/dot/traversal path components structurally; returns FileTree (not a ByteView) |
| xdvdfs (Xbox) | disc filesystem | T2 | **GREEN** (2026-08-20, `substratum/formats/xdvdfs.py`; real Jade Empire retail proof) | `FIXTURE REQUEST` — real XGD1 retail image `Jade Empire (Japan).iso` in gitignored `fixtures/_local/` (`sha256` pinned locally; no retail bytes in git), plus an independent oracle (`xdvdfs-rs 0.9.0`) listing/extraction; the parser is green only after the independent tree and byte-range proof matches on the real disc | synthetic via `seedtools/make_xdvdfs_fixture.py` (`fixtures/xdvdfs/synthetic/`) + retail proof on `fixtures/_local/Jade Empire (Japan).iso` with `base_offset=0x18300000` (descriptor at `0x18310000`) and `xdvdfs-rs 0.9.0` as the differential oracle | none new (stdlib `struct`) | parser fix path: XDVDFS descriptor offset may be embedded (`base_offset + 0x10000`), left/right LCRS entries are 4-byte dword counts and must be multiplied by 4 before walking; real retail proof passes on Jade Empire with independent oracle agreement (path set and extracted bytes match `xdvdfs unpack`), while structural rejects remain closed on bad magic/tail, bad root table, out-of-bounds ranges, invalid names, and cross-table cycles |
| 3ds-cci | container (partition table) | T2 | **GREEN** (2026-07-26, `substratum/formats/three_ds_cci.py`) | Gitignored **Cubic Ninja (Japan).3ds** metadata anchor (`CTR-P-AQNJ`, title ID `0004000000034300`, 134,217,728 bytes, sha256 `929a675e…`); partition 0 CXI (offset 16,384, 86,430,720 bytes, sha256 `b805cdfd…`) and partition 7 CFA (offset 86,447,104, 5,116,416 bytes, sha256 `ee478b03…`); `stage_3ds_cci_retail_anchor.py` commits only provenance + manifest while retail/extracted bytes stay local | ctrtool **v1.3.0** supplies independent NCSD/NCCH IDs, bounds, and sizes + 3dstool **v1.2.6** independently extracts both complete partition payloads; direct source-range hashes must match those extractions; both tools are pinned/provisioned by `vendor_tools.py 3ds`; fidelity sample seed **1** (both files) | none left | NCSD/CCI outer layer only: returns a FileTree of opaque original-image slices and does not parse NCCH regions, ExeFS, or RomFS. Refuses media-size mismatch, half-empty/no-partition tables, overlapping/out-of-bounds partitions, zero/duplicate partition IDs, empty-slot IDs, NCSD/NCCH ID or size disagreement, and bad NCSD/NCCH magics. |
| 3ds-ncch | container (section table; decrypted only) | T2 | **GREEN** (2026-07-26, `substratum/formats/three_ds_ncch.py`) | partition 0 CXI independently extracted from the Cubic Ninja CCI above (86,430,720 bytes, sha256 `b805cdfd…`); product `CTR-P-AQNJ`, NoCrypto, executable/application; `stage_3ds_ncch_retail_anchor.py` commits only provenance + manifest while all four extracted region references stay local | ctrtool **v1.3.0** supplies header flags, exact region bounds, and GOOD extended-header/ExeFS/RomFS protected hashes + 3dstool **v1.2.6** independently extracts the complete extended-header/plain/ExeFS/RomFS regions; each extraction's complete hash must match its direct source range; fidelity sample seed **1** (all four files) | none left (both tools vendored by `vendor_tools.py 3ds`) | Decrypted NCCH layer only: returns a FileTree of present opaque regions (extended header including access descriptor, plain, logo, ExeFS, RomFS); inner ExeFS/RomFS traversal is future caller composition. Supports production format versions 0 and 2; refuses prototype v1, encrypted/seeded NCCH, content-size drift, half-empty/overlapping/out-of-bounds regions, invalid extended-header/protected spans, and declared-hash mismatch. Protected hashes stream in bounded chunks. The fixture's NoCrypto-modified NCCH RSA header signature is known non-original and is not treated as content corruption. |
| wii-disc | container (encrypted partition table) | T2 | **GREEN** (2026-07-26, `substratum/formats/wii_disc.py`) | Gitignored **The Munchables (USA).iso** (`RQCEAF`, NTSC-U, revision 0, 4,699,979,776 bytes, sha256 `64c012f3…`); two encrypted/signed partitions: UPDATE at `0x50000` (187,334,656 bytes, sha256 `61338bbb…`) and DATA at `0x0f800000` (4,424,728,576 bytes, sha256 `0c1af607…`); `stage_wii_disc_retail_anchor.py` commits only provenance + manifest while the disc and complete opaque references stay local. Scrubbed unused blocks make it unsuitable as a pristine whole-disc hash anchor, but both partitions verify and content extraction is exact | pinned wit **v3.05a r8638** `dump`/`verify` supplies independent disc identity, partition offsets/sizes/types/signing state, and `+OK` verification for both partitions; complete direct source-range copies must match recorded SHA-256; qualification cross-check: Dolphin **2606** agreed on identity and all 61 extracted source/system files (1,690,942,404 bytes); fidelity sample seed **1** (both complete slices) | none left (wit vendored) | Unkeyed outer layer only: returns a FileTree of opaque encrypted DATA/UPDATE/CHANNEL partition slices, deriving each end from its cleartext data-offset/data-size fields. Does not derive keys, decrypt clusters, or walk the Wii FST. Refuses bad Wii magic, empty/half-empty/overlapping/out-of-bounds or excessive partition tables, duplicate/unknown types, zero/bad partition offsets, malformed TMD/certificate/H3 metadata ranges, overlaps, and out-of-bounds partitions. |
| wii-partition | container (AES-CBC decode) | T2 | **GREEN** (2026-07-29, `substratum/formats/wii_partition.py` + `substratum/_aes.py`) | synthetic generated-key partition fixture (`fixtures/wii_partition/synthetic/`, 8 clusters, seedtool-authored with a **generated** test key `0123...cdef` — NOT the retail Wii key; the test key and known-plaintext descriptor commit) + conditional retail proof against The Munchables DATA partition (`fixtures/wii_partition/munchables/`, gitignored decrypted references from pinned wit `extract` at 3 fixed decrypted offsets: boot.bin@0x0, bi2.bin@0x440, apploader.img@0x2440 — ~255 KB spanning a cluster boundary; fst.bin/main.dol excluded as variable-position, wii-fst's layer); no key bytes or decrypted retail payloads commit. Key procedure: [`docs/WII-KEYED-WORK.md`](docs/WII-KEYED-WORK.md) | NIST **SP 800-38A Appendix F.2.1** AES-128-CBC vectors anchor the pure-Python AES independently of any Wii fixture + wit **v3.05a r8638** `extract` is the independent retail decrypt oracle; fidelity sample = all 8 synthetic clusters + 3 retail fixed-offset regions | local operator-supplied raw 16-byte standard Wii common-key file (index 0), named by `SUBSTRATUM_WII_COMMON_KEY_FILE`; generated test key for the synthetic path | Input is one encrypted partition SliceSource from `wii-disc`; returns one lazy decrypted `ByteView` over 0x7C00-byte cluster payloads (135,028 clusters / 4,088 MB on Munchables DATA). Key boundary: `SUBSTRATUM_WII_COMMON_KEY_FILE` names an exact 16-byte local file, fails closed when absent/wrong-sized, and is never logged/hashed/echoed. Runtime stays stdlib-only per DESIGN §4: the pure-Python AES-128-CBC in `substratum/_aes.py` (precomputed GF(2^8) tables, ~0.5 MiB/s bulk); no external executable at runtime. Ticket@0 → encrypted title key@0x1BF (IV = titleID@0x1DC + zeros, common key idx@0x1F1) → per-cluster IV@0x3D0. Refuses bad ticket signature type, non-standard key index, half-empty/non-cluster-aligned data region, out-of-bounds reads. **Perf fix (2026-08-14, Stratum consumer ask):** reads decrypt only their own 16-byte blocks (CBC *decryption* chains through ciphertext, so the preceding ciphertext block is the XOR IV; the cluster IV seeds block 0) with a once-per-source key schedule — the 1-read-per-file census over MKWii's 2007 files dropped **139.4 s → 0.61 s (228×)**; `materialize()` adds the decrypt-once temp-file spool (context-managed, idempotent close, `weakref.finalize` fallback) for bulk consumers; the old bounded 4-cluster LRU was removed as strictly dominated by block-granular reads. Sequential bulk throughput is data-bound and unchanged (~0.5 MiB/s; a one-time `materialize()` of a 4.4 GB partition costs ~2.4 h single-threaded, then plain reads). |
| wii-fst | disc filesystem | T2 | **GREEN** (2026-07-29, `substratum/formats/wii_fst.py`) | synthetic decrypted DATA partition (`fixtures/wii_fst/synthetic/`, 5-entry nested FST: 3 files + 2 dirs, seedtool-authored with known payloads; committed) + retail proof against The Munchables DATA partition (`fixtures/wii_fst/munchables/`, wit's independent `files-ll` listing authors the expected manifest; 50 files + 3 dirs = 53 entries; gitignored decrypted reference bytes from wit `extract`); no key bytes or decrypted retail payloads commit | wit **v3.05a r8638** `files-ll` (independent listing → manifest) + `extract` (independent decrypted reference bytes → fidelity); fidelity sample seed **1** (16 files, bounded 64 KiB head+tail per file under pure-Python AES) | `wii-partition` GREEN (composes wii-disc → wii-partition → wii-fst) | Decrypted DATA-partition filesystem layer only: walks the FST into a `FileTree` and leaves partition crypto to caller-visible composition. **Load-bearing Wii-format finding: FST file offsets are word offsets (byte = value << 2), NOT byte offsets like GameCube** — this is why wii-fst stays independent of gc-fst rather than sharing the walker (the offset transform is baked into traversal). FST located via header field 0x424 (word-shifted offset) / 0x428 (word-shifted size). Node format is otherwise identical to GC (0x0C-byte nodes, yagcd §13.4). Sniffer is deliberately weak (no true magic on a decrypted partition) — three-gate heuristic: printable disc-ID + not a GC disc + plausible FST location; explicit `format="wii-fst"` is the reliable dispatch. Refuses bad root type, zero/out-of-bounds FST size, file ranges exceeding the decrypted size, and invalid FST path components. |
| 3ds encrypted-ncch / cia | keyed/install containers | T2/T3 | **encrypted-ncch GREEN** (2026-07-29, `substratum/formats/three_ds_ncch_enc.py`; plain-7.x widening + Kobayashi anchor 2026-07-30); CIA parsing is now its own GREEN unit (see the `cia` row below) | two encrypted-NCCH retail anchors in gitignored `fixtures/_local/`: **Biohazard — The Mercenaries 3D (Japan)** `.cia` (698,837,440 bytes, title ID `0004000000043E00`, standard crypto `Secure (0)` keyslot `0x2C`, NCCH content slice at file offset `0x3900` / `0x29A6F600` bytes, `stage_3ds_ncch_enc_retail_anchor.py`) + **Kobayashi ga Kawai Sugite Tsurai!! (Japan)** `.3ds` CCI (2,147,483,648 bytes, title ID `0004000000168700`, **plain-7.x `Secure (1)` no-seed** keyslot `0x25`, NCCH = partition 0 sliced via the NCSD table, `stage_3ds_ncch_enc_plain7x_retail_anchor.py`). Committed synthetic (`fixtures/3ds_ncch_enc/synthetic/decrypted.ncch`) is a NoCrypto NCCH covering the structural + composition paths — a committed encrypted fixture the production ctrtool can decrypt is NOT authorable without retail key material (`make_3ds_ncch_enc_fixture.py` documents the finding) | ctrtool **v1.3.0** (vendored; **retail keys compiled in**) as runtime decryptor + ctrtool-decrypt vs **3dstool v1.2.6**-decrypt two-party differential (the genuine oracle — ctrtool `-p` is a tautology); NCCH-declared protected hashes are the independent correctness anchor; fidelity sample seed **1** (all regions, streamed) | vendored ctrtool (no external key for standard or plain-7.x crypto); `aes_keys.txt` parked at gitignored `fixtures/_local/` for a future pure-Python path (not consumed by ctrtool v1.3.0) | **Architecture: ctrtool-at-runtime → decrypted `ByteView` → caller composes through existing `three_ds_ncch`** (the `chd`→`iso9660` pattern applied to 3DS). No-seed encrypted scope: `ncchflag[3]` in `{0x00, 0x01}` (standard `0x2C` + plain-7.x `0x25`); refuses 9.3 (`0x0A`)/9.6 (`0x0B`) and the seed bit (`0x20`); 7.x-seed is a separate module (header-encrypted, CIA-consuming). Sniffer is registered **before** `3ds-ncch` so encrypted content (whose NCCH magic is plaintext) dispatches to decryption, not the decrypted-only sibling. Load-bearing findings: (1) the NCCH header at 0x100 is **plaintext** for standard AND plain-7.x crypto (only 7.x-seed encrypts the header itself); (2) ctrtool's `-p`/plain flag returns encrypted bytes (and errors on the encrypted ExeFS header), so it is NOT a differential — the real two-party oracle is ctrtool-decrypt vs 3dstool-decrypt; (3) no committed encrypted synthetic is authorable without retail key material (3dstool `--fixed-key` debug key ≠ ctrtool retail keys), so the committed fixture is a decrypted NoCrypto NCCH and the decrypt+differential is carried by the retail anchors. **Plain-7.x two-party refinement (2026-07-30):** ctrtool and 3dstool agree on `.code`/`icon`/`plain`/`logo`/`extendedheader`/`romfs` content; 3dstool **strips a banner signature region** (~207 bytes at banner+0x130 / +0x1ff) that ctrtool preserves — a documented tooling difference, not a decrypt disagreement (the ExeFS protected-hash region is the 0x200 header, which both agree on). Decrypted titlekey ctrtool prints is never recorded; no key bytes or decrypted retail payloads enter git. |
| cia | install container | T2 | **GREEN** (2026-07-29, `substratum/formats/three_ds_cia.py`) | synthetic two-content CIA (`fixtures/3ds_cia/synthetic/`, seedtool-authored; committed manifest + reference bytes) + retail metadata anchor **Biohazard — The Mercenaries 3D (Japan)** `.cia` (698,837,440 bytes, single content) — `stage_3ds_cia_retail_anchor.py` commits only provenance + manifest while the on-media encrypted content blob stays gitignored | ctrtool **v1.3.0** supplies the independent CIA identity (title ID, content size, NCCH recognition); the TMD content-chunk record's declared SHA-256 is the correctness anchor (unencrypted chunks: on-media bytes; CDN-encrypted chunks: titlekey-decrypted blob, matching ctrtool `-y`); fidelity sample seed **1** (all sections, streamed) | none new (stdlib `struct`/`hashlib` + existing `_aes.py` key generator; CDN-encrypted eShop CIAs need `SUBSTRATUM_3DS_KEYSET_FILE`) | CIA outer container only: a `FileTree` of opaque section slices (header/certs/ticket/tmd/content.\<idx\>.ncch per TMD record/footer). Section layout is header-driven with **64-byte per-section alignment** (content lands at `align64(header+cert+ticket+tmd)`, verified = 0x3900 on Biohazard). The TMD (3DBrew layout: RSA-2048 sig region + fixed header + content-chunk records at `0x9C4+sig`, each `0x30` bytes) supplies per-content index/size/hash; **full multi-content support** (each chunk 64-aligned, hashed against its TMD record — decrypt-then-hash when TMD type bit 0 is set, using `slot0x3DKeyX`+`commonN` via the hardware key generator). The FileTree still exposes on-media slices; it does not parse NCCH content or validate TMD RSA signatures — a caller composes `3ds-ncch-enc` then `3ds-ncch`. Retail eShop proof 2026-08-21: BoxBoxBoy + Mini Sports (both two-content, CDN-encrypted) verify against TMD hashes after titlekey decrypt; Biohazard (flag clear) stays keyless. Refuses bad header size/type/version, zero content, sections that do not tile the file, TMD content-chunk records that do not tile the content section, duplicate/non-ascending content indices, content hash mismatch, and overlapping sections structurally. |
| 3ds-ncch-enc-seed | container (decode; 7.x-seed) | T2 | **GREEN** (2026-07-29, `substratum/formats/three_ds_ncch_enc_seed.py`) | retail anchors **BoxBoxBoy! (USA) (eShop)** `.cia` (94,181,696 bytes, title ID `000400000018ee00`) primary + **Mini Sports Collection (USA) (eShop)** `.cia` (69,691,712 bytes) secondary in gitignored `fixtures/_local/`; both `Secure (1) (KeyY seeded)`, keyslot `0x25`; `stage_3ds_ncch_enc_seed_retail_anchor.py` commits only provenance + manifests while decrypted region references stay gitignored | ctrtool **v1.3.0** (vendored; 7.x keyslot `0x25` compiled in) + operator-supplied **seeddb** (`SUBSTRATUM_CTRTOOL_SEEDDB`, gitignored `fixtures/_local/seeddb.bin`); the NCCH-declared protected SHA-256 hashes are the correctness anchor (ctrtool `-y` GOOD + runtime compose through `three_ds_ncch`); 3dstool CANNOT serve as a second decryptor (handles neither the CIA nor a raw 7.x-seed slice — both fail on the encrypted header) | vendored ctrtool + parked seeddb (env-var boundary, presence-only reporting, never hashed/logged/committed) | **Architecture: ctrtool-at-runtime (whole CIA + `--seeddb`) → decrypted NCCH `ByteView` → caller composes through `three_ds_ncch`.** Separate module from `3ds-ncch-enc` because the input is a **whole CIA**, not a raw NCCH slice: the 7.x-seed variant **encrypts the NCCH header itself** (magic at 0x100 is ciphertext), so ctrtool cannot decrypt a raw slice in isolation — it needs the CIA's ticket to decrypt the header first. ctrtool exposes the decrypted regions but not the decrypted header, so the normalizer **reconstructs the 0x200 header** (region table + protected hashes + NoCrypto flag) from ctrtool's parsed report. Scope: 7.x-seed (`Secure 1 KeyY seeded`) only; plain 7.x (no seed), 9.3, 9.6 stay deferred. Two retail anchors (BoxBoxBoy + Mini Sports) catch variant bugs. Key discipline: seeddb via env var, presence-only reporting, never hashed/logged/committed; no decrypted retail payloads enter git. |
| 3ds-romfs | filesystem (decrypted RomFS region) | T2 | **GREEN** (2026-08-21, `substratum/formats/three_ds_romfs.py`) | synthetic via `seedtools/make_3ds_romfs_fixture.py` (`fixtures/3ds_romfs/synthetic/` — hand-packed: real IVFC hash tree, real GBATEK-checksum hash buckets, variable-length dir/file entries with inline UTF-16LE names, a 0-byte member, a one-block member, and a partial trailing block; expected manifest authored from the seedtool layout per the xdvdfs pattern, with ctrtool carrying the differential in tests); retail anchor **Cubic Ninja (Japan)** `.3ds` staged in gitignored `fixtures/_local/` — the skipif test composes the full `3ds-cci` → `3ds-ncch` → romfs-region chain and matches ctrtool's own extraction (540 files, exact path/size map + byte-exact spot checks) | ctrtool **v1.3.0** (vendored; `-t romfs --listromfs`/`--romfsdir` on the level-3 slice as the independent oracle — synthetic AND retail) + eager SHA-256 hash-tree verification as the structural anchor; fidelity sample seed **1** (all files) | `3ds-ncch` GREEN (input is the decrypted romfs.bin region from the NCCH tree; the encrypted variants decrypt to the same layer) | Walks the IVFC-wrapped RomFS region into a FileTree whose byte ranges point directly into the region (no spool). **Load-bearing layout finding (characterized 2026-08-21 on retail media, agreeing with ctrtool's "Actual" offsets):** the declared level offsets describe the canonical pre-data chain [0, align(l0), align(l0)+align(l1)], but the 3DS RELOCATES — level-3 data sits at align(header+master, block) = 0x1000 and the level-0/level-1 tables follow AFTER the data, level-1's padded end exactly the region end; closed-form identities tie every table size to its block count (s0 = ceil(s1/B)·0x20, s1 = ceil(sdata/B)·0x20, mhs = ceil(s0/B)·0x20). The complete hash tree is verified eagerly (master ← L0 ← L1 ← data; partial trailing block zero-padded) — a single flipped payload byte is a structural red. Names are genuine UTF-16LE (JP titles carry non-ASCII; valid Unicode accepted, canonically escaped in manifests — a documented deviation from the ASCII-only units). Refuses bad magic/version/header-size, non-canonical declared chains, size-identity or hash mismatches, non-tiling metadata tables, cyclic dir/file sibling chains, parent-link disagreement, duplicate paths, traversal/dot/control-character path components, and file ranges outside the data level. **ctrtool v1.3.0 quirk (recorded):** its extractor cannot materialize 0-byte members — the synthetic reference stages `EMPTY.BIN` empty and the two-party test extracts a no-empty rebuild; normalize itself handles 0-byte members. |
| 3ds-ncch-enc-96 | container (decode; New3DS 9.6/9.3, pure-Python) | T2 | **GREEN** (2026-07-30, `substratum/formats/three_ds_ncch_enc_96.py`) | committed synthetic (`fixtures/3ds_ncch_enc_96/synthetic/encrypted.ncch`, seedtool-authored with **generated test** keyX/keyY — NOT retail; exercises the real decrypt path AND the two-key ExeFS split) + retail anchor **Fire Emblem Warriors (USA) (v0.0)** `.3ds` CCI (2,147,483,648 bytes, title ID `000400000f70cd00`, `ncchflag[3]==0x0B`, keyslot `0x1B`, **no-seed** — seed bit clear, `seedcheck=0`); partition 0 NCCH sliced via the NCSD table (offset `0x4000`, declared size `0x747c8000`); only the committed synthetic + a generated test keyset enter git, retail bytes stay gitignored | **pure-Python AES-128-CTR** (`substratum/_aes.py`, NIST SP 800-38A F.5.1 anchored) + the 3DS hardware key-generator (`normalkey_from_keyxy`, C1 pinned from `dnasdw/3dstool` source); correctness anchor = NCCH-declared protected SHA-256 hashes validated by composing the decrypted `ByteView` through `three_ds_ncch` (proven on FE Warriors retail bytes); 3dstool CANNOT second-party 9.6 (no working `0x1B` decrypt path); fidelity sample seed n/a (lazy/windowed decrypt, never materializes the ~1.9 GB romfs) | operator-supplied **keyset** (`SUBSTRATUM_3DS_KEYSET_FILE`, gitignored `fixtures/_local/aes_keys.txt`) carrying `slot0x2CKeyX` + `slot0x1BKeyX`; env-var boundary, presence-only reporting, never hashed/logged/committed | **Architecture: pure-Python CTR decrypt → NoCrypto NCCH `ByteView` → caller composes through `three_ds_ncch`.** Bypasses vendored ctrtool v1.3.0 entirely (it cannot decrypt keyslot `0x1B`). **LOAD-BEARING — the two-key NCCH model:** a 9.6 NCCH does NOT decrypt all regions with one key. Key0 (`slot0x2C`, derived from keyY=sig[:16]) encrypts the extended header, the ExeFS superblock, and the ExeFS tail; Key1 (`slot0x1B`) encrypts the first ExeFS file (`.code`) and the entire RomFS. ExeFS is one continuous CTR stream whose key switches mid-stream (superblock/.code/tail); counter block indices are region-relative. No-seed 9.6 only (`0x0B` + seed bit clear, e.g. FE Warriors); seeded-9.6 (seed bit set) and 9.3 (`0x0A`/`0x18`, no anchor — lost media) are the same code path once a title exists. Lazy source with a bounded 4-window LRU cache (peak RSS low regardless of romfs size); plaintext NoCrypto header bit presented to the composed layer. |
| zip | container (archive-as-FS) | T2 | **GREEN** (2026-08-21, `substratum/formats/zip.py`) | synthetic via `seedtools/make_zip_fixture.py` (`fixtures/zip/synthetic/` — hand-packed, no zipfile module: stored + deflate members over sha256-chain and patterned payloads, a 0-byte member, dir entries, a data-descriptor member with zeroed local sizes, a ZIP64 member carrying 0xFFFFFFFF sentinels + extra-field sizes in both headers, and a ZIP64 EOCD/locator chain in front of an EOCD with a comment; CD order deliberately differs from path order). Expected manifest authored by `seedtools/author_zip_manifest.py` from 7-Zip's independent listing, and reference bytes = 7-Zip's own `7z x` extraction — never tree.read output (two-party rule) | 7-Zip **26.02 (x64) 2026-06-25** (`7z t` fixture validation, `7z l -slt` listing → expected manifest, `7z x` → reference bytes) + **stdlib zlib** raw-deflate decode; fidelity sample seed **1** (all 7 files) | none new (stdlib zlib/struct) | DEFLATE is not seekable, so the layer returns a FileTree over a **one-file decompression spool** (the `chd` pattern) with a documented, load-bearing layout: file payloads spooled in ascending path order, back to back (the manifest's offsets are authored against that rule — a CD-order spool mutant dies at the gate). Per the house convention for spooled containers the manifest's `source.size`/`sha256` describe the spool, not the archive. Validates the EOCD/ZIP64-EOCD chain with cross-checks, central-vs-local agreement (magic, name, method, flags, sizes incl. local ZIP64 sentinels), and per-member CRC-32 + inflated size during the streaming spool (1 MiB chunks). Spool owned by the tree's source (idempotent `close()`, context management, `weakref.finalize` fallback). Refuses encrypted members, unsupported compression methods, multi-disk archives, ZIP64 sentinels without resolvable records, member data at or past the central directory, duplicate paths, non-ASCII names without the UTF-8 flag, EOCD/CD entry-count disagreement, and empty/dot/traversal/backslash/absolute/colon path components structurally. The census consumption shape (per-title extract → scan → delete) stays the consumer's composition. |
| rvz | container (decode) | T2 | **GREEN** (2026-08-21, `substratum/formats/rvz.py`) | retail `fixtures/_local/` `GT Cube (Japan).rvz` (GC, 273,494,128 bytes → 1,459,978,240 bytes ISO, 661 `gc-fst` files) + `Ghost Squad (Japan).rvz` (Wii, 374,616,064 bytes → 4,699,979,776 bytes ISO, 2 `wii-disc` partitions); no committed synthetic — retail anchors cover both platforms (`P-GTCJ`/`RGSJ8P`) | DolphinTool **2606a** (`dolphin-2606a-x64.7z` sha256 `4c58045f9821cb63913f4df08ea86ece3cdda9f9e646154516000fa1547e0c37`, `DolphinTool.exe` sha256 `98f0a7d2e711eeb53a3504d453eb970f4178e48dcb4c745e3d8fa24d8d90a6bc`) `dolphin-tool convert --format iso` + `wit v3.05a r8638` for inner `gc-fst`/`wii-disc` | `dolphin-tool` vendored via `seedtools/vendor_tools.py` (`tools/dolphin-tool/DolphinTool.exe`) | Returns `ByteView` over DolphinTool's `RVZ→ISO` decode (RVZ magic `RVZ\x01`, block `131072` `zstd:5`); spool via `mkdtemp` + `weakref.finalize` (like `chd`/`cso`/`zip`); caller composes `rvz → gc-fst` (GC) or `rvz → wii-disc → wii-partition → wii-fst` (Wii); full `zip → rvz → gc-fst` chain now covers the 2,573-title GC+Wii corpus (`BACKLOG.md:359`). |
| gcz | container (decode) | T2 | **GREEN** (2026-08-21, `substratum/formats/gcz.py`) | `fixtures/_local/` `GT Cube (Japan).gcz` (GC sub_type 0, 1,115,451,806 bytes → 1,459,978,240 bytes ISO, 661 `gc-fst` files; sha256 `a14f114bb10b7caea234d82c46986634514f05b10a40f6d5e9b07132a8ce0910`, written by pinned DolphinTool 2606a `-f gcz -b 16384` from the staged retail `.rvz` — the `chd`/BursTrick create-from-retail precedent) + `Ghost Squad (Japan).gcz` (Wii sub_type 1, 4,445,970,537 bytes → 4,699,979,776 bytes ISO, 2 `wii-disc` partitions; sha256 `54f9d1b8bd844737fac19ec58713a46bb264152b37f3fb265eff4c48c616f5f3`) + `Beach Spikers - Virtua Beach Volleyball.gcz` (operator-staged **mislabeled** file — a compacted raw GC ISO, no `0xB10BC001` anywhere in it; kept as the sniff/dispatch regression, `gc-fst` walks it with 1,224 files) | DolphinTool **2606a** (the same pinned vendored exe + hashes as `rvz`) + **spec-derived pure-Python block decoder in `tests/test_gcz.py`** — wit 3.05a refuses GCZ outright (`WRONG FILE TYPE`, recorded), so the container differential is a decoder authored from the on-disk layout (32-byte LE header `magic\|sub_type\|compressed_data_size u64\|disc_size u64\|block_size\|num_blocks`, `u64[num]` offset array with bit-63 stored-raw flag, `u32[num]` hash array, then zlib blocks; `32 + 12·num_blocks + compressed_data_size == file_size` holds to the byte on both anchors), agreeing with DolphinTool's decode on head/mid/tail 1 MiB windows; plus the full-disc `nested/game.iso → gcz → decode` round-trip is sha256-identical | shares `tools/dolphin-tool/DolphinTool.exe` via `substratum/formats/_dolphin.py` (plumbing extracted from `rvz` in this unit; `rvz` re-exports its test surfaces unchanged) | Returns `ByteView` over DolphinTool's `GCZ→ISO` decode (magic `0xB10BC001`, sub_type 0=GC/1=Wii); caller composes `gcz → gc-fst` (GC) or `gcz → wii-disc` (Wii). Corpus note: `F:\game` holds no GCZ titles (GC/Wii are `.rvz`) — this unit is emulator-family completeness per the follow-on decision; the operator's staged `.gcz` sample turned out to be a plain compacted ISO already served by `gc-fst` (and a `cri.adx` positive candidate for Stratum — loose `adx/bgm_*.adx` throughout). |
| wbfs | container (decode; scrubbed) | T2 | **GREEN** (2026-08-21, `substratum/formats/wbfs.py`) | retail `fixtures/_local/` `Ghost Squad (Europe).wbfs` (operator-staged, 1,096,810,496 bytes, sha256 `f17e764dbe0984766a5ddd8497da87f0f60eff5c4610a6838f6e00a05d4c5cca`, disc `RGSP8P`; scrubbed single-layer Wii — no update partition, 522 of 4,482 LUT clusters present; the JP `.rvz` anchor is the same title unscrubbed) | **wit v3.05a r8638 cygwin64** (`wit copy` → canonical 4,699,979,776-byte reconstruction, zero-filled scrubbed clusters) + **spec-derived pure-Python LUT decoder in `tests/test_wbfs.py`** as the container differential — **load-bearing finding: DolphinTool 2606a's WBFS decode is mangled and NOT usable as an oracle** (2x disc size 9,399,959,552, the WBFS container header embedded verbatim at 0x200000, partition bytes disagreeing with wit); the libwbfs-layout reconstruction (BE header scalars, disc_table at 0x0C, disc_info at hd-sector 1 with 0x100 header copy + BE-u16 wlba LUT, `(143432*2) >> (wbfs_sec_shift-15)` entries) hashes **sha256-identical to wit's decode over the entire 4.7 GB image**, streamed (no full-disc materialization; the disc_info header copy cross-checks disc[0:0x100]) | wit vendored at `tools/wit/wit.exe` (+cygwin DLLs; `SUBSTRATUM_WIT` override, PATH, repo candidate); spool via shared `substratum/formats/_spool.py` (extracted from `_dolphin.py` this unit) | Returns `ByteView` over wit's reconstruction; caller composes `wbfs → wii-disc → wii-partition → wii-fst`. Scrub semantics are inherent to the format: junk (and on this anchor the update partition) is absent and zero-filled — the unit guarantees partition layout + encrypted payloads, never original junk bytes. Single-disc files only (disc slot 0; `.wbf1` split sets out of scope). `F:\game` holds no WBFS titles (Wii are `.rvz`) — emulator-family completeness, same as `gcz`. |
| nkit | container (recover; GC nkit.iso) | T2 | **GREEN** (2026-08-21, `substratum/formats/nkit.py`) | retail `fixtures/_local/` `Yu-Gi-Oh! The Falsebound Kingdom (Europe).nkit.iso` (operator-staged, 440,041,472 bytes, sha256 `4c471bf5dc624c65dafe3f8f179b12697df22c0b1067a8452dd26c328e7a8b86`, disc `GYFPA4`) + round-trip anchor `Mario Kart - Double Dash!! (USA).iso` (identity-verified retail `GM4E01`, already staged for `wii-partition`/`wii-fst`) | **NKit 1.4** (Nanook; `ConvertToISO.exe` — the last public NKit release: **NKit 2 is Discord-distributed with no public source, recorded**; the zip sha256 `e600b0a2dbacf784779ce33b01259f197c5f8cfc7135f1831160d3f98bb431c6` is pinned against the independent AUR package pin) + **byte-exact retail round-trip**: `MKDD.iso → ConvertToNKit (VerifySuccess) → normalize_nkit` is sha256-identical to the original over the full 1,459,978,240 bytes + the tool's own 3-pass Full Verify (`VerifySuccess`) as structural anchor + recovered-vs-compacted `gc-fst` file-map agreement (415 entries, identical path→size maps) | NKit 1.4 vendored via `seedtools/vendor_tools.py nkit` (`tools/nkit/`, referer-gated gbatemp fetch; config's `WaitForKeyAfterProcessing` flipped to false); per-call isolated ~1 MB tool copy inside the temp spool (the tool hardcodes `<exe-dir>/Processed` with no output flag) | **GC `.nkit.iso` only**: a compacted GameCube image (real GC disc header at 0, `NKIT` signature block at 0x200, FST rewritten) — the compacted form is directly file-level walkable by `gc-fst` (415 files; the Backlog finding), and this unit returns the RECOVERED original (junk regenerated, original FST order restored) for byte-fidelity consumers. Sniff = `NKIT`@0x200 corroborated by the GC disc magic at 0x1C; **registered before `gc-fst`** (an nkit dispatching there would silently serve the compacted layout). Load-bearing tooling notes: a verified conversion exits NONZERO when the CRC misses the (empty) Redump/Custom dats — the produced image is the success criterion, never the exit code; `.nkit.gcz` wraps the compacted image in a Dolphin-GCZ stream, so it sniffs/decodes via `gcz` to the compacted image (file-level correct; not recovered). Wii NKit (RVT-H-ish) has no sample — out of scope. |

**Dispatch order updated 2026-07-30 (9.6 GREEN):** the encrypted-NCCH family is
now complete for all available anchors — `3ds-ncch-enc` (standard `0x00` +
plain-7.x `0x01`), `3ds-ncch-enc-seed` (7.x-seed), and `3ds-ncch-enc-96`
(New3DS 9.6 `0x0B`, pure-Python). The 9.6 unit bypasses ctrtool entirely
(vendored v1.3.0 cannot decrypt keyslot `0x1B`): it reads `slot0x2CKeyX` +
`slot0x1BKeyX` from the operator keyset, derives the two normal keys via the
hardware key generator, and CTR-decrypts in pure Python with the **two-key
model** (exheader/ExeFS-superblock/ExeFS-tail = Key0; `.code`+RomFS = Key1).
FE Warriors is the no-seed `0x0B` anchor; the committed synthetic (generated
test keys) exercises the real decrypt path + the ExeFS key split.

New3DS 9.3 (`0x0A`, keyslot `0x18`) — **REMOVED from the backlog
2026-08-21 by user decision**: the tooling falls out of the 9.6 path
(same module, `slot0x18KeyX`), but a 2026-07-30 hunt confirmed genuine
`0x0A` titles are effectively lost media (three database-"9.3" titles all
qualified as `0x01`), and chasing one is not realistic without
full-corpus download capacity. **The firmware version a title requires
is unrelated to its NCCH crypto method** — read ctrtool `-v`'s `Flags:` byte
(the 4th byte = `ncchflag[3]`), not the FW. Full detail in
`docs/3DS-KEYED-WORK.md` § "CORRECTION (2026-07-30)" and the two-key finding in
[`docs/3DS-PURE-PYTHON-AES-CTR-PLAN.md`](docs/3DS-PURE-PYTHON-AES-CTR-PLAN.md).


**PS1 streaming proof (2026-07-26):** the frozen four-check semantics and
fixed-width XA view are unchanged, but equal verifier chunks now bypass the
byte-wise search and `ps1-bincue` validates/reads bounded 512-sector raw
batches. On the local 157,246-sector BursTrick anchor, normalization improved
from 6.181793 s to 0.356493 s, an equal 110,608,384-byte comparison from
4.104896 s to 0.109350 s, and the complete retail gate passed in 1.81 s.
Exact first-difference offsets, raw-read counts, batch bounds, and corruption
diagnostics at the first batch boundary are regression-tested.

**PS1 header-integrity proof (2026-07-26):** the synthetic carrier starts
at 00:00:00, while both local retail anchors start at the standard 00:02:00;
all three have valid BCD fields and advance by exactly one frame per sector.
Runtime now accepts either origin while refusing invalid BCD digits, seconds
at or above 60, frames at or above 75, and any sequence discontinuity. The
extra pass costs about 0.53 microseconds per sector: normalization measured
29.74 ms versus 22.56 ms on the 12,916-sector King's Field anchor and
389.03 ms versus 306.02 ms on the 157,246-sector BursTrick anchor.

**Saturn/Dreamcast streaming proof (2026-07-26):** eager validation and
cooked reads now use bounded 512-sector raw batches while preserving complete
per-sector sync/mode validation. On the 83-sector synthetic fixture,
normalization improved from 0.005048 s to 0.000352 s and a complete cooked read
from 0.004524 s to 0.000313 s. Raw-read counts, batch bounds, byte identity, and
exact corruption diagnostics at sector 512 are regression-tested.

**Saturn/Dreamcast real-sector proof (2026-07-26):** the openly licensed
Save Game Copier 3.7.1 release adds a third-party-authored 843,776-byte ISO
that expands to 412 complete raw sectors. Pinned ECM independently classifies
all 412 as Mode 1 with zero literal bytes and canonical ECM/UNECM
round-tripping preserves the 969,024-byte carrier at SHA-256
`8392e7d6f6e9606ba91b502191dc0ee9972fbd729e41697c26a098e35f7a239e`.
Substratum's decoded stream byte-equals the upstream release. The synthetic
carrier was also regenerated through the same independent reconstructor, so
its EDC and P/Q ECC are no longer zero-fill placeholders.

**Saturn/Dreamcast integrity-scope benchmark (2026-07-26):** complete
stdlib-only EDC, reserved-field, and P/Q ECC validation measured
487–504 microseconds per sector across the two committed carriers, projecting
about 162 seconds for a 333,000-sector disc. EDC alone was about
220 microseconds/sector and ECC about 271 microseconds/sector, so eager
runtime error-code enforcement was rejected. BCD-valid, monotonically advancing
MSF header validation measured 0.4–0.7 microseconds/sector in the prototype and
adds about 0.24 ms to normalization of the 412-sector homebrew anchor; it is now
the runtime structural boundary. Arbitrary valid starting MSF is accepted so
session/track extracts remain representable.

**CISO final-slot layout (2026-08-21, `ciso`):** block 2241 covers only the
last 262,144 bytes of the 4,699,979,776-byte disc, so a writer could store a
short final slot. wit does not, and wit's reader cannot read one. The layout
was unobservable — `wit copy` scrubs the disc tail on every real image, and
block 2233 is the highest ever seen present across Mario Kart Wii, Sonic
Colors and The Munchables (all three exactly `_TOTAL` bytes) — so it was
settled by construction: both candidates were built from a wit-authored Mario
Kart Wii CISO by marking block 2241 present and appending its real disc bytes.
wit decodes the full-block form and reproduces the disc tail exactly; on the
short form it dies with `ERROR #84 [READ FILE FAILED] in ReadCISO() @
src/lib-ciso.c#398, Read failed [F=3,2982182912+2097152]`, asking for a whole
block at the slot base. Slot accounting is therefore uniform
(`len(present) * block_size`). Until this measurement it truncated the final
slot, which inverted the polarity exactly — the file wit reads was refused as
trailing garbage and the file wit refuses was accepted — unreachable only
because no staged sample has a present block 2241. Pinned both ways by
`tests/test_ciso.py`. Note also that when block 2241 *is* present, wit emits
2242 whole blocks (4,701,814,784 bytes); the ByteView keeps the true
single-layer size and treats the extra 1,835,008 as block padding outside the
disc address space.

**Recursion depth of the tree walkers (measured 2026-08-21, audit D2):**
`gc-fst`, `wii-fst`, `wii-u8-arc` and `3ds-romfs` recurse once per directory
nesting level (sibling chains are `while` loops), and `xdvdfs` recurses on
both the left child and the right sibling of its LCRS binary tree, so only
its depth is untied from the directory depth. Measured against
`sys.getrecursionlimit()` = 1000: **max nesting depth 3** across every
committed manifest (chd/ciso/cso/iso9660/ps1-bincue at 3, everything else at
2 or below), and LCRS depth **6** on the synthetic xiso (9 entries) and **29**
on the Jade Empire retail pressing (4,022 entries — roughly 2x a balanced
tree's ~12 levels). Reaching the limit would need a near-degenerate tree of
order 100,000 entries in one directory, which optical media does not produce.
Recorded as standing assertions in `tests/test_recursion_depth.py` rather than
as a one-off note, so a future fixture that changes shape fails with the
number instead of surfacing as a mystery `RecursionError`. An explicit-stack
rewrite of five GREEN, retail-proven walkers was declined on this evidence;
the gate already turns `RecursionError` into a structural red.

Prep rows (explicitly-scoped, not normalizer units) — **all DONE
through 2026-07-26** via `seedtools/vendor_tools.py` (binaries live in gitignored
`tools/`; the committed script re-fetches and verifies against its pinned
sha256s, so the repo carries the provenance, not the bytes):

The provisioner also pins the extracted executable SHA-256 values and
requires exact complete banners (not substring matches). Downloads have
a 60-second socket timeout; all external tool invocations have bounded
timeouts, with a regression scan covering runtime and seedtools.

- `vendor-chdman` — chdman **0.288** (banner `manager 0.288 (mame0288)`),
  extracted with 7-Zip from the official MAME 0.288 Windows SFX
  (`mame0288b_x64.exe`, sha256 `e4ae20a2…` matching upstream SHA256SUMS;
  SFX never executed) → `tools/chdman/chdman.exe`. GPL-2.0+ as a linked
  whole (chdman.cpp itself BSD-3) — vendored locally, never committed.
- `vendor-dolphin-tool-or-wit` — **wit v3.05a r8638** chosen over
  dolphin-tool: listing carries sizes+offsets (`wit files-ll`),
  self-contained cygwin64 zip, real `wit version`; dolphin-tool lists
  bare names, has no version flag, and needs the VC++ redistributable.
  Zip sha256 `04967055…` (TOFU, upstream publishes no sums) →
  `tools/wit/` (wit.exe + cygwin DLLs). GPL-2.0.
- `vendor-maxcso` — maxcso **v1.13.0** (own code ISC; bundled libs
  MIT/BSD/zlib/Apache-2.0, 7-zip deflate LGPL), the 64-bit Windows release
  `maxcso_v1.13.0_windows.7z` (7z sha256 `51362619…`, TOFU — upstream
  publishes no sums), flat-extracted with 7-Zip → `tools/maxcso/maxcso.exe`.
  Run-only, never linked or committed (same posture as chdman);
  `maxcso --version` → `maxcso v1.13.0`. Authors the `cso` fixture and is
  its `--decompress` structural anchor.
- `vendor-3ds-tools` — ctrtool **v1.3.0** + 3dstool **v1.2.6**,
  independently maintained CCI/NCSD/NCCH readers. Their official Windows
  release archives and extracted executables are SHA-256 pinned;
  `vendor_tools.py 3ds` retains only the two executables and never
  downloads or vendors an external key file.
- `vendor-ecm` — ECM/UNECM from the GPL-2.0 **v1.3.1** Windows release
  asset (zip sha256 `c480d553…`) → `tools/ecm/`. The pinned executables
  report **v1.3.0**, an upstream packaging mismatch recorded rather than
  normalized away. Both executable hashes and both exact banners are
  enforced. Run-only, never linked or committed.
