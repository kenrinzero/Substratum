# Code audit — 2026-08-21-b (two sessions since AUDIT-2026-08-21)

- **Date:** 2026-08-21
- **Target:** the two production sessions added after the audited head `f061cef` that `AUDIT-2026-08-21.md` covered:
  - **CIA decrypt-then-hash** (`dd85ab8` — `substratum/formats/three_ds_cia.py` + `tests/test_3ds_cia.py`)
  - **CHD retail closure** (`e24b0ab` docs + `6082203` — `substratum/formats/chd.py` + `substratum/formats/ps1_bincue.py`)
  Per the user's scoping, the intervening surfaces `zip` (`8f9d4b6`), `three_ds_romfs` (`57a3896`) and the romfs-test hoist (`d93e92d`) are **excluded from this memo** — they remain unaudited by this memo and are not re-verified here.
- **Languages:** Python 3.13 (stdlib-only runtime per `AGENTS.md` §4)
- **Taxonomy:** `code-audit` checklist, all 24 entries in order (`C:\Users\kenrin\.agents\skills\code-audit\checklist.md:1`)
- **Prior audit memos:** `AUDIT-2026-07-26.md`, `AUDIT-2026-07-26-b.md`, `AUDIT-2026-07-30.md`, `AUDIT-2026-08-14.md`, `AUDIT-2026-08-21.md`; repeated shapes marked below.
- **Reviewed head:** `6082203` (`fix(chd): retail closure — _TempFileSource.path + ps1-bincue duck-typed sibling resolution`), clean tree, `main == origin/main` per `log.md:206`
- **Gate evidence:** `uv run pytest -q` → **432 passed in 0:13:35** at both `dd85ab8` and `6082203` (log.md:175,206); no skipped tests — both eShop CIA retail proofs (BoxBoxBoy, Mini Sports) and both CHD retail proofs (PSP DVD-type 7 Wonders, CD-type BursTrick round-trip) executed locally.

---

## Findings by entry

### 1. swallowed-exceptions

**no candidates / legitimate-variant (repeat-class)** — checked `except` sites in the in-scope files (`three_ds_cia.py:277`, `chd.py:124,192,210`):

- `three_ds_cia.py:277` `except ValueError as exc: raise ValueError(...) from exc` re-raises with chaining — matches the checklist's "Re-raise after logging" false-positive guidance (`checklist.md:34`). Not swallowed; preserves traceback.
- `chd.py:124` `except (subprocess.CalledProcessError, OSError, subprocess.TimeoutExpired): return None` matches the "function returning `None` from inside an `except`" cue (`checklist.md:21`), but is the documented tool-boundary fallback: `_chd_tag`'s docstring (`chd.py:111-114`) says "fall back to the safe CD extraction path instead of failing early" and the caller `_chd_extract_command` (`chd.py:137-164`) then takes the `extractcd` branch which re-fails loudly (`check=True`) on genuinely broken media. Triaged legitimate-variant per `AUDIT-2026-08-21.md:52-57` and `AUDIT-2026-08-14.md:40`; the `TimeoutExpired` arm itself was the real defect fixed in `9ab291c` and is now correct — verified via AST that it catches the precise `subprocess.TimeoutExpired` type, not the dead builtin `TimeoutError`.
- `chd.py:192,210` `except BaseException: ... raise` both clean up (`unlink` / `shutil.rmtree`) then re-raise — checklist's "Re-raise after cleanup" case, not swallowed.
- `verify.py:88` frozen gate `except Exception` remains legitimate-variant (repeat, `AUDIT-2026-08-21.md:52`); not in scope but re-verified unchanged.

No `except: pass`, no `except Exception: return None/False`, no `logger.debug` in except, no `# noqa: BLE001` at scale in the new surface.

### 2. hardcoded-config-values

**no candidates — checked** — grepped numeric literals in API calls and string resource names in `three_ds_cia.py`, `chd.py`, `ps1_bincue.py`:

- New literals are named protocol constants (`three_ds_cia.py:49-94` — `_CIA_ALIGN=0x40`, `_HEADER_SIZE_FIXED=0x2020`, `_TMD_SIG_RSA2048_SHA256=0x10004`, `_TICKET_TITLEKEY_OFFSET=0x7F`, etc.; `chd.py:67` `_CHDMAN_TIMEOUT_SECONDS=300`; `ps1_bincue.py:69-83` sector geometry). All fail loudly if wrong (header size mismatch, sig type refusal, sector batch bounds) — the defect shape is silent-truncation values like `max_tokens=64000` (`checklist.md:61`).
- Streaming chunk `min(1 << 20, ...)` at `three_ds_cia.py:240,337` and `chd.py:186` is the same 1 MiB buffer used in `verify.py` and other keyed layers; it is a performance/memory bound, not a silently-truncating API default, and changing it does not alter correctness — legitimate per `checklist.md:59-61` "genuinely-best-default" / "named constant would fail loudly" guidance. Prior audits did not flag this shared `1<<20` pattern.
- No `config.get("key", MAGIC_NUMBER)`, no two declarations of the same default, no omitted config that silently accepts an upstream default.

### 3. missing-network-timeout

**no candidates — checked** — `three_ds_cia.py` has no network/URL/subprocess calls; `chd.py` has the only two `subprocess.run` sites in scope:

- `chd.py:117` `_chd_tag` → `timeout=_CHDMAN_TIMEOUT_SECONDS`
- `chd.py:201` `normalize_chd` → `timeout=_CHDMAN_TIMEOUT_SECONDS`

Both pass a named timeout; the AST guard `tests/test_tooling_hardening.py` that enforces this invariant remains green, and the new test-oracle timeout added in `9ab291c` (`tests/test_xdvdfs.py:139`) is outside this memo's scope but was re-verified present via grep. `urllib.request.urlopen` in `vendor_tools.py:117` still carries `DOWNLOAD_TIMEOUT_SECONDS` (prior audits). No `requests.get`, `httpx.get`, or bare `fetch` in the new surface.

**not applicable** for network-specific sub-cues (no `fetch`/`axios`/`socket.accept` in this Python codebase).

### 4. narrating-comments

**false-positive-shape (repeat) / no candidates** — scanned comments in the three files; none matches "comment immediately above a line that restates the line in English" (`checklist.md:103`):

- `three_ds_cia.py:1-33` module docstring explains the non-obvious TMD-hash-of-decrypted-blob contract, titlekey IV semantics, and alignment tiling — "Comments explaining non-obvious constraints" (`checklist.md:111`).
- `three_ds_cia.py:370` and `chd.py:30-37` / `ps1_bincue.py:405-412` document the `base_offset`/`lba_base` composition and the new `path` duck-typing — protocol/phase explanations, not scaffolding.
- No `# Step 1:` scaffolding, no `Args:` paraphrases, no section banners dividing a file into named sections in the new code (the `# --- resolve a filesystem path` at `chd.py:176` is a phase boundary within a single function, permitted per `checklist.md:115`).

Triage: permitted dense provenance comments, same class as `AUDIT-2026-08-14.md:64-72` and `AUDIT-2026-08-21.md:84-88`.

### 5. near-identical-siblings

**no candidates — checked** — threshold is "three or more sequential blocks differing only in one identifier" (`checklist.md:136`):

- `three_ds_cia.py` repeats `struct.unpack_from`/`pack_into` with different format strings/offsets, but each handles a distinct section (header vs TMD vs ticket) with different endianness and validation; collapsing would obscure format-specific refusal rules.
- `chd.py:135-164` two `extractdvd` vs `extractcd` branches differ in command, output naming (`.bin` vs `.cue`/`.bin` sibling), and semantics — not near-identical.
- No three duplicate handler registrations; no `if/elif` chain with identical bodies.

Prior `AUDIT-2026-08-21.md:91-93` and `AUDIT-2026-07-30.md:67-79` triaged the `_TempFileSource` triple as intentional duplication so each decode layer stays dispatchable — same rationale holds; the new `self.path` line does not create a sibling cluster.

### 6. convention-drift

**legitimate-variant (repeat-class) — closable, not a real defect** — sibling normalizer surface stays uniform:

- All normalizers still expose `sniff(source: ByteSource) -> bool` + `normalize_<format>(source) -> ByteView|FileTree` with one-layer returns (`three_ds_cia.py:109,347`, `chd.py:103,167`, `ps1_bincue.py:390,429`; registry at `normalize.py:82-114`). No verb mixing (`getX`/`fetchX`), no import-style mixing.
- `chd.py:42` `_TempFileSource.path` and `ps1_bincue.py:414` `hasattr(source, "path")` widen the accepted composition input from strict `FileSource` to any `.path`-bearing `ByteSource`. This closes the `chd → ps1-bincue` composition refusal flagged as uncategorized observation 1 in `AUDIT-2026-08-21.md:241-253`. Triage: legitimate-variant — defensible reason (the CHD extraction temp dir's sibling `.cue` is only resolvable via a path; duck-typing makes the CHD layer composable without a contract change, documented in both docstrings). Prior `lba_base`/`base_offset` extra-param deviation was similarly triaged legitimate-variant (`AUDIT-2026-08-21.md:96-102`); the dispatcher `normalize(source, *, format=None)` stays uniform.

No mixed relative/absolute imports, no `require()` vs `import`.

### 7. inconsistent-error-handling

**legitimate-variant (repeat)** — checked `raise` shapes across the three files (`three_ds_cia.py:129-441`, `chd.py:80-208`, `ps1_bincue.py:114-463`):

- Malformed media → `ValueError` (CIA: bad header size, zero content, `sections do not tile`, TMD too small, `hash mismatch`; PS1: bad sync, BCD range, MSF contiguity; CHD: `extracted.cue` mismatch via `_CueError` subclass of `ValueError`).
- Absent operator machinery:
  - CHD → `FileNotFoundError` (`_CHDMAN_ENV` empty/missing, `which` miss, repo fallback miss at `chd.py:80,84,96`) — direct file-not-found.
  - CIA keyset → `ValueError` (`_KEYSET_ENV is not set`, `points to a missing file`, `not found in the keyset` at `three_ds_cia.py:259,265,288`; invalid hex/length at `:278,283`). This matches the established 3DS keyed pattern: `_load_keyx` in `three_ds_ncch_enc_96.py:130-154` likewise raises `ValueError` for the same env-var cases. `AUDIT-2026-08-14.md:110-114` already triaged the `FileNotFoundError` vs closed `ValueError` naming the env var as legitimate-variant ("genuinely distinct failure classes"); the new CIA follows the ValueError side consistently with its sibling 3DS modules, so no new inconsistency.
- No sibling wrappers mix tuple returns with wrapper-helper returns; no divergent error shapes for the same failure class.

### 8. mutable-default-arguments

**no candidates — checked** — AST/regex scan of `substratum/formats/three_ds_cia.py`, `chd.py`, `ps1_bincue.py` finds no `=[]`, `={}`, `=set()`, `=dict()`, `=list()` defaults, no `Optional[container] = <empty>` and no `Field(default_factory)` misuse (`checklist.md:216-220`). New defaults are not present; `normalize_3ds_cia(source)` has no default, `_content_iv(index: int)` etc. take immutable ints/bytes.

### 9. assert-for-runtime-validation

**false-positive-shape (repeat) / no new candidates** — `three_ds_cia.py` and `chd.py` add no `assert`; grep for `assert <condition>` in the in-scope production files returns only `ps1_bincue.py`'s three pre-existing sites:

- `ps1_bincue.py:79` `assert SYNC_LEN + HEADER_LEN + XA_SUB_LEN + USER_LEN + EDC_LEN + ECC_LEN == SECTOR` — module-level layout identity over developer-authored constants (`checklist.md:251`), not external data.
- `ps1_bincue.py:325` `assert self._cache_sector is not None` after `if index == self._cache_i:` — type-narrowing after a branch that guarantees assignment; prior padding ensures the cache was populated.
- `ps1_bincue.py:493` `assert origin_msf is not None` after loop `for batch_start in range(0, n_data, _RAW_BATCH_SECTORS):` where `origin_msf` is set on first iteration and `n_data` is proven `>0` at `ps1_bincue.py:461-465` (`if n_data <= 0: raise`). All three were triaged `false-positive-shape` in `AUDIT-2026-07-26.md:80-83` and `AUDIT-2026-08-14.md:125-131`; no new production `assert` validates TMD bytes, header fields, or user input.

### 10. async-await-mismatch

**not applicable** — no `async def`, `await`, promises, or event loop in `substratum/`, `seedtools/`, or `tests/` (`checklist.md:253`); grep for `async ` and `await ` clean.

### 11. brittle-error-detection

**legitimate-variant (repeat-class)** — `chd.py:129` `re.search(r"Tag='([^']*)'", line)` steers dispatch on `chdman info` stdout:

- Source is a pinned foreign CLI (`chdman 0.288`, `NORMALIZERS.md:21`, `BACKLOG.md` operator-verified `Tag='DVD '` whitespace-padded), no typed API — the checklist's "Genuine bridging to a non-typed source" (`checklist.md:305`) and the `AUDIT-2026-08-21.md:128-141` precedent for ctrtool parsing.
- Unexpected wording degrades to the CD fallback (`_chd_tag` returns `None` → `extractcd` path) which then fails loudly (`check=True` at `chd.py:203`) on DVD media rather than silently mis-extracting.
- Grep for `in str(e)` / `in str(exc)` / `e.message.includes` across the in-scope runtime: clean. `three_ds_cia.py` and `ps1_bincue.py` add no substring steering.

### 12. f-string-in-logger-call

**not applicable** — no `logging` import or `logger.<level>(...)` calls in `substratum/` or `seedtools/` (`checklist.md:310`); grep for `import logging`, `getLogger`, `logger\.` in the three files clean.

### 13. print-instead-of-logging

**no candidates — checked** — `grep -rn "print("` in `substratum/` returns nothing; `three_ds_cia.py`, `chd.py`, `ps1_bincue.py` contain zero `print(` calls. Seedtool `print` remains confined to `if __name__ == "__main__"` entry points (repeat, `AUDIT-2026-07-30.md:142-144`), not in scope for this memo.

### 14. resource-leak-no-context-manager

**false-positive-shape (repeat) / no candidates** — checked handle ownership:

- `three_ds_cia.py:270` `with path.open("r", encoding="ascii", errors="replace") as fh:` — context-managed; no streaming handles held as instance state; `_stream_hash` / `_stream_hash_cbc` are stateless streaming helpers, not owners.
- `chd.py:181` `tempfile.NamedTemporaryFile(suffix=".chd", delete=False)` + `197` `tempfile.mkdtemp(prefix="substratum-chd-")` intentionally use `delete=False`/`mkdtemp` because `chdman` requires a named path and the decoded image must outlive the call. Every failure path cleans: `182-195` `except BaseException: unlink` for the staged CHD, `210-212` `except BaseException: shutil.rmtree` for `tmp_dir`, and success ownership passes to `_TempFileSource` (`chd.py:30-62`) which supplies idempotent `close()` plus `weakref.finalize(tmp_dir, ignore_errors=True)` — triaged false-positive-shape in `AUDIT-2026-07-26.md:109-114`, re-verified with the one-line `self.path` addition not touching lifecycle.
- `ps1_bincue.py:198` `cue_path.read_text(...)` uses `Path.read_text` which opens/closes internally; no leak.

No `open(...)` outside `with`, no `pickle.dump(path.open(...))`, no streaming handle as instance var without `__enter__/__exit__`.

### 15. shell-true-subprocess-injection

**no candidates — checked** — `grep -rn "shell=True\|os\.system\|os\.popen"` in `substratum/` clean; the only `subprocess.run` calls are `chd.py:117,201`, both pass an argument list (`[str(_chdman_exe()), "info", "-i", str(chd_path)]` / `command` from `_chd_extract_command`), never an f-string or concatenated shell command. `three_ds_cia.py` has no subprocess at all.

### 16. sleep-based-synchronization

**no candidates — checked** — no `time.sleep`, `asyncio.sleep`, or JS `setTimeout` promise-sleep in the three files; grep for `sleep` clean. The `tests/conftest.py:81` RSS sampler cadence (`AUDIT-2026-08-14.md:167`) is outside this memo's scope and remains the checklist's "Deliberate pacing" false-positive.

### 17. string-built-sql

**not applicable** — no `cursor.execute`, no SQL client, no query construction (`checklist.md:442` "only if the codebase constructs SQL — otherwise not applicable"); grep for `SELECT|INSERT|execute\(` shows only `re.search` and `digest.update` false hits.

### 18. swapped-args

**no candidates — checked** — verified every multi-arg positional site in the new code against its signature:

- `FileEntry(path=..., kind=..., offset=..., size=...)` at `three_ds_cia.py:407-412,427-434` uses **keyword arguments**, matching `contract.py:84-89` field order (`path, kind, offset, size`) — same shape `AUDIT-2026-08-14.md:192-198` triaged as correct.
- `struct.unpack_from("<I", header, _HEADER_SIZE_OFFSET)` etc. all use `<I`/`>I`/`>H`/`>Q` matching the header/TMD/ticket endianness documented at `three_ds_cia.py:33,87,90` (CIA header little-endian, TMD/ticket big-endian per 3DBrew); `struct.pack_into(">H", iv, 0, index)` and `">Q"` for title_id likewise.
- `cbc_decrypt_blocks(round_keys, iv, ciphertext)` at `three_ds_cia.py:321` `cbc_decrypt_blocks(expand_key(common_key), bytes(iv), enc_titlekey)` and `341` `cbc_decrypt_blocks(round_keys, prev_iv, chunk)` pass `(round_keys: expand_key, iv: bytes, ciphertext: bytes)` in documented order (`substratum/_aes.py:258-280`); no IV/ciphertext swap.
- `expand_key(key)` / `normalkey_from_keyxy(keyx, keyy)` at `three_ds_cia.py:299-300,321,333` respect keyX/keyY order.
- `ps1_bincue.py:414-417` `Path(source.path)` / `Path(source)` order matches `_resolve_pair` param; sibling call `_chd_extract_command(chd_path, tmp_dir)` at `chd.py:200` matches `(chd_path: Path, tmp_dir: Path)`.

No two call sites to the same function use conflicting positional order.

### 19. tarfile-extractall-without-filter

**no candidates — checked** — `grep -rn "extractall"` in `substratum/` + `seedtools/` clean; no `tar.extractall` / `ZipFile.extractall`. The only stdlib archive reader in the in-scope files is not present; the unrelated `zipfile` use in `seedtools/vendor_tools.py` is the allow-listed provisioning path (`AUDIT-2026-07-30.md:195-198`).

### 20. unjustified-lazy-import

**no candidates — checked** — live AST scan of `substratum/formats/three_ds_cia.py`, `chd.py`, `ps1_bincue.py` plus `tests/test_3ds_cia.py` finds **zero function-body imports**; all stdlib/heavy imports are module-scope (`three_ds_cia.py:38-45` `hashlib, os, struct, dataclasses, pathlib, substratum._aes, contract`; `chd.py:17-23` etc.). The `d93e92d` handover hoisted the 4 regressed imports in `tests/test_3ds_romfs.py` (`SliceSource`, `importlib`, `dispatch`), re-verified with `ast.walk` repo-wide clean. Matches `AUDIT-2026-08-21.md:192-195` remediation.

### 21. unreachable-defensive-guard

**false-positive-shape (pre-existing) / no new candidates** — checked guard clusters:

- `three_ds_cia.py:187,309` `if sig_size is None: raise` — validates TMD/ticket signature type against allow-list after reading on-media bytes (`tmd[0:4]` / `ticket[0:4]`) — value originates at serialization boundary (on-media TMD), checklist permits "Guards on values crossing a serialization boundary" (`checklist.md:558`).
- `three_ds_cia.py:196-204` `if content_count==0`, `if records_end > len(tmd)` — structural tiling of TMD-declared count against actual TMD size.
- `three_ds_cia.py:379-393` `if content_cursor + rec.size > content_end` / `if content_cursor != content_end` — TMD-declared chunk sizes vs content section tiling; all attacker-controllable.
- `three_ds_cia.py:416-417` `if title_key is None: raise` is the only guard that is strictly unreachable under current logic (if `any(encrypted)` then `title_key` is assigned at `399`). Triage: **false-positive-shape**, same class as `ps1_bincue.py:493` `assert origin_msf is not None` (type-narrowing after a loop that unconditionally assigns when `n_data>0` guard at `461` holds) — defensive fail-closed, narrow scope, does not hide a reachable malformed-input path; converting to an unconditional assignment+else would be churn with no behavior change. The `AUDIT-2026-08-21.md:198-207` `xdvdfs.py:89-90` unreachable-silently-skips was more severe (would contradict refuse-don't-guess); this one raises.

No `isinstance` re-check on already-typed parameters; no `if x is None: return None` over an internal helper whose callers guarantee non-None.

### 22. weak-test-assertion

**no candidates / false-positive-shape (gate contract) — checked** — audit of `tests/test_3ds_cia.py` (the only test file changed in scope):

- Retail eShop proofs pin **exact** sizes, not loose bounds: `test_retail_boxboxboy_cdn_encrypted_cia_is_green` (`test_3ds_cia.py:479-481`) asserts `by_path["content.0000.ncch"].size == 0x5956000` and `0x78000`; Mini Sports at `489-490` asserts `0x424F000` / `0x24000` — exact, matching the TMD-declared sizes (`checklist.md:573` loose-bound cue does not fire). Prior `AUDIT-2026-08-14.md:261-269` real defect `len(adx) > 100` is not repeated.
- Structural-red tests use exact `pytest.raises(ValueError, match=...)` or `match="hash mismatch"` / `"SUBSTRATUM_3DS_KEYSET_FILE is not set"` etc. (`test_3ds_cia.py:291-344,452-465`), not substring `in content.lower()` on rendered HTML.
- Encrypted wrong-key test `test_encrypted_wrong_common_key_is_hash_mismatch` (`test_3ds_cia.py:460-465`) asserts the exact `hash mismatch` reason, not "either outcome."
- The one intentionally broad assertion is `test_wrong_slice_mutant_dies_at_manifest_or_fidelity` (`test_3ds_cia.py:387-411`) which asserts `any(problem.startswith(("manifest:", "fidelity:", "structural:")) for problem in problems)` — this is the **gate self-test** class the checklist excludes: test name promises only that the mutant dies somewhere in the four-check gate, not a specific stage (`checklist.md:580` "Smoke tests named to match their intent"). Same shape as `AUDIT-2026-07-26.md:188-191` and `AUDIT-2026-08-21.md:216` which triaged gate self-tests/wrong-slicer mutants as false-positive-shape. Recorded here as documented false-positive, not a real defect.

No `or` chains in assertions, no `assert result in (a,b)` for specific-behavior tests, no HTML substring matches.

### 23. off-by-one

**no candidates — checked** — scanned `len(`, `range(`, `parents[`, `+1`/`-1` boundaries in the three files:

- `three_ds_cia.py:208` `for i in range(content_count):` iterates TMD-declared count; slicing at `209-212` uses `records_base + i*0x30 : records_base+(i+1)*0x30` with exclusive end, bounds-checked at `200` `if records_end > len(tmd)`.
- `three_ds_cia.py:240,337` streaming `min(1 << 20, size-consumed)` uses chunked `consumed += len(chunk)` not `+1`; no `len(collection)/duration` rate calc, no display-width `len(string)` for CJK/emoji, no `Path.parents[N]` beyond `Path(__file__).resolve().parent.parent.parent` at `chd.py:71`.
- `ps1_bincue.py:373,474,478` `for batch_index in range(batch_count)` / `for batch_start in range(0, n_data, _RAW_BATCH_SECTORS)` are standard batch loops with `batch_count = min(_RAW_BATCH_SECTORS, n_data - batch_start)` — exclusive-end, correct by construction (same pattern benchmarked in `AUDIT-2026-07-26.md:223-229`).
- No `range(0, len(arr)-1)` skip, no `N frames / N-1 intervals` rate bug.

### 24. wrong-tool-for-job

**no candidates — checked** — beyond entry-1's fallback (filed there), no mis-applied primitive:

- CIA uses `cbc_decrypt_blocks(expand_key(...), iv, data)` from `substratum/_aes.py` (NIST-anchored AES-128-CBC) for both titlekey decrypt and content decrypt-then-hash — correct tool vs shelling out to `ctrtool` (which cannot decrypt eShop titlekeys without the hosted keyset; pure-Python is the intended second-party for this layer).
- CHD dispatch uses tag → `extractdvd`/`extractcd` selection (`chd.py:138`) instead of blanket `extractcd` — right primitive (`AUDIT-2026-08-21.md:232`).
- PS1 duck-typing (`ps1_bincue.py:414` `hasattr(source, "path")` → `Path(source.path)`) is `pathlib`, not string-concatenated `'dir/' + name`, and not `os.path.join` where `pathlib` is already in scope — correct tool per `checklist.md:627`.
- No `str.format` HTML building, no single-item bulk loops, no `raise Exception(...)` where a typed error exists (all `ValueError`/`FileNotFoundError`/`RuntimeError` are typed).

---

## Compound findings

None. No two real defects interact at a single site in the new surface. The fixed `subprocess.TimeoutExpired` arm (prior entry 1) and the `chd.py:42` + `ps1_bincue.py:414` composition closure are independent; fixing one does not fix the other, and the combination is not worse than either alone.

## Uncategorized observations

1. **Version docs lag the code by one bump (repeat of `AUDIT-2026-08-21.md:294-295` observation 5, now with new content).** `substratum/__init__.py:11` and `pyproject.toml:6` still say `0.0.21`; `NORMALIZERS.md:21-33` and `BACKLOG.md` already describe the `0.0.22` CHD retail closure with dual proofs (7 Wonders DVD-type 22 files + BursTrick CD-type round-trip). The gate and runtime are `0.0.22`-correct; the package metadata is stale by the one commit `6082203`. Not a checklist defect (hardcoded-config-values asks about silent API defaults, not package version drift), but worth a T3 sync (`uv lock` already `0.0.21` at `pyproject.toml:6`).

2. **`chd.py:_TempFileSource.path` is a new public attribute outside the `ByteSource` protocol (`contract.py:24-30`).** `ByteSource` is `read_at`/`size` only; `FileSource` adds `.path` as a convenience. The new `chd.py:42` `self.path = path` makes `_TempFileSource` the second `.path`-bearing `ByteSource`, and `ps1_bincue.py:414` now relies on this via duck typing. This is intentional and documented (`chd.py:34-37`, `ps1_bincue.py:405-412`), but it widens the informal contract: any future `ByteSource` that happens to carry a `.path` attribute named after something else (e.g., a logical path vs filesystem path) would be mis-resolved. Mitigated because only `_TempFileSource` and `FileSource` produce filesystem paths in this codebase, and both are `Path` objects.

3. **CIA exposes on-media ciphertext while the TMD hash is of plaintext (`three_ds_cia.py:1-10,428`).** The `FileTree` entries for `content.XXXX.ncch` are opaque slices into the original CIA; `read(entry)` returns ciphertext for CDN-encrypted chunks while `_stream_hash_cbc` verifies the TMD's SHA-256 against the titlekey-decrypted stream internally. The module docstring (`three_ds_cia.py:25-30`) states this explicitly, and `tests/test_3ds_cia.py:433-450` asserts both that `tree.read(c0) != plaintext` and that decrypt-then-hash succeeds — but a downstream consumer unfamiliar with the convention could mistake the `content.*.ncch` bytes for plaintext. No defect per taxonomy; the `cia` row in `NORMALIZERS.md:33` also documents it as "The FileTree still exposes on-media slices; it does not parse NCCH content."

4. **Positive findings, for the record.** The CIA fix correctly identifies the root cause (TMD hashes titlekey-decrypted bytes, not on-media ciphertext) and implements the narrow decrypt-then-hash path: titlekey derived via `slot0x3DKeyX`+`commonN` through the hardware key generator (`three_ds_cia.py:294-321`, reusing `substratum/_aes.py:359-379`), per-content CBC IV as `content index as BE u16` (`three_ds_cia.py:248-252`), streaming 1 MiB decrypt-and-hash (`_stream_hash_cbc` at `324-344`) with correct chaining `prev_iv = chunk[-_AES_BLOCK:]`, validated block-multiple check at `328-331`, and synthetic + two retail proofs (BoxBoxBoy 94,181,696 bytes, Mini Sports 69,691,712 bytes) with distinct content counts — honestly proven and key-discipline-clean (env-var `SUBSTRATUM_3DS_KEYSET_FILE`, presence-only errors at `288-291`, keys never logged; `tests/test_3ds_cia.py:233-238` helper uses `tmp_path` with generated keys). The CHD closure's retail proofs are also honest: `chdman info` Tag parsing is whitespace-tolerant (`tag.strip() == "DVD"` at `138`), DVD-type composes via `extractdvd → iso9660` (no cue expected, log-proven `7 Wonders` 22 files), CD-type proves `BursTrick` byte-identical through both paths.

## Suggested fix order

No real defects were found in the in-scope production surface (`three_ds_cia.py`, `chd.py`, `ps1_bincue.py` and the delta in `tests/test_3ds_cia.py`). The list below is therefore empty; the uncategorized observations are optional T3 hygiene, not correctness/security fixes.

*No real-defect fixes to order.* If the operator chooses to address the uncategorized notes, sync `__version__`/`pyproject.toml` to `0.0.22` first (one-line version bump, then `uv lock`), then consider the `.path` protocol note only if a future `ByteSource` gains a conflicting `.path`.

False-positive-shape and legitimate-variant findings need no fixes.

## Summary

- **real-defect:** 0 (0 in the two new sessions; the two real defects from `AUDIT-2026-08-21.md` — dead `TimeoutError` arm + unbounded oracle subprocess — remain resolved at this head, re-verified)
- **false-positive-shape:** 6 (entry 1 re-raise, entry 4 provenance comments, entry 9 `ps1_bincue` module-level + narrowing asserts, entry 14 `chd` temp ownership, entry 21 serialization-boundary guards, entry 22 gate broad mutant)
- **legitimate-variant:** 3 (entry 1 `chd` tool-boundary fallback, entry 6 duck-typed `.path` composition closure, entry 7 operator-machinery `ValueError` vs `FileNotFoundError` split, entry 11 chd `Tag=` foreign-CLI bridge)
- **not applicable:** 3 entries (10 `async-await-mismatch`, 12 `f-string-in-logger-call`, 17 `string-built-sql`)
- **no candidates:** 14 entries (2, 3, 5, 8, 13, 15, 16, 18, 19, 20, 23, 24, plus the no-new-candidate halves of the mixed entries above)
- **uncategorized observations:** 4

Every entry in the 24-entry checklist was covered for the in-scope surface (`three_ds_cia.py`, `chd.py`, `ps1_bincue.py` and the delta in `tests/test_3ds_cia.py`). The out-of-scope `zip`/`three_ds_romfs` surfaces introduced at `8f9d4b6`/`57a3896`/`d93e92d` were not audited by this memo per the user's scoping. The correctness and security picture from `AUDIT-2026-08-21.md` holds at `6082203` for the reviewed surface: no swallowed exceptions, no missing runtime timeouts, no archive extractall, no shell injection, no key leakage, no lazy-import regression, no retail bytes in git, and no weak retail assertions.
